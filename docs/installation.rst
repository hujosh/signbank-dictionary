============
Installation
============

At the command line::

    $ easy_install signbank-dictionary

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv signbank-dictionary
    $ pip install signbank-dictionary
